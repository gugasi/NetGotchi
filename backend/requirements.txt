# Pinned versions. Floating deps are a supply chain attack surface.
# Run `pip-audit -r requirements.txt` in CI to catch known CVEs.
fastapi==0.115.0
uvicorn[standard]==0.30.0
websockets==14.0
pydantic==2.10.0
